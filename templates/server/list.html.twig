{% extends 'layout.html.twig' %}
{% set section = 'admin' %}
{% set subSection = 'servers' %}

{% set libKilikTable = 1 %}

{% block pageContent %}
    <div class="row">
        <div class="col-lg-12">
            <div class="card shadow mb-4">
                <div class="card-body">
                    {% embed "server/_list.html.twig" with {"table": table} %}
                        {% block tableTitle %}
                            <div class="btn-group" role="group" aria-label="...">
                                <a href="{{ path("server_add") }}" class="btn btn-sm btn-outline btn-info"><i class="fa fa-plus" aria-hidden="true"></i> {{ 'action.addAServer' | trans | capitalize }}</a>
                            </div>
                        {% endblock tableTitle %}
                    {% endembed %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascript %}
    {{ parent() }}
{% endblock javascript %}
